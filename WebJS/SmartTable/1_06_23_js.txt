1. Страница отображает людей в таблице: Фото (путь к фото), ФИО, адрес, телефон, зарплата
2. Пользователи могут выполнять следующие функции:
- добавить человека (в отдельном div, содержащем форму)
- редактировать человека (в отдельном div, содержащем форму)
- удалить человека из таблицы (radio, checkbox)
- при вводе данных валидировать их при помощи регулярных выражений

3. Разработать редактор графа, который имеет следующие функции:
- добавление/удаление вершин
- перемещение вершин
- добавление/удаление рёбер
- задание для вершин и рёбер надписей

OOP

4.

Разработать объект Employee, имеющий следующие члены
    - FIO, address, phone
	- Salary
    - print()
	
Разработать объект Group, имеющий следующие члены
    - employees[]
    - print()
    - addEmployee
	- removeEmployee
	- setEmployee
	- getEmployee
	
5.

Разработать объект SmartTable, имеющий следующие методы:
 - SmartTable(x, y, objects[]) - конструктор принимает координаты таблицы и массив пользовательских объектов
 - show()
 - hide()
 - add(obj)
 - remove(obj)
 
 Умная таблица сама определяет свойства помещённых в неё объектов и формирует на основе этого столбцы таблицы